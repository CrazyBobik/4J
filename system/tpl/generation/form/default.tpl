<div class="row">
    <label for="{id}" class="row-item">{title}</label>

    <div class="row-item">
        <input class="in in-text" type="{type}" name="{name}" value="{value}" id="{id}">
    </div>
</div>